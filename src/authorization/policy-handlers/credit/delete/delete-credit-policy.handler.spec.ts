import { DeleteCreditPolicyHandler } from './delete-credit-policy.handler';

describe('DeleteCreditPolicyHandler', () => {
  it('should be defined', () => {
    expect(new DeleteCreditPolicyHandler()).toBeDefined();
  });
});
