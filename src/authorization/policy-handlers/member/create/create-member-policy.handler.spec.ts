import { CreateMemberPolicyHandler } from './create-member-policy.handler';

describe('CreateMemberPolicyHandler', () => {
  it('should be defined', () => {
    expect(new CreateMemberPolicyHandler()).toBeDefined();
  });
});
