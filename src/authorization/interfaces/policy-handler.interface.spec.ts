import { PolicyHandlerInterface } from './policy-handler.interface';

describe('PolicyHandlerInterface', () => {
  it('should be defined', () => {
    expect(new PolicyHandlerInterface()).toBeDefined();
  });
});
